# TODO: create profile test and run tests with it??
spring:
  datasource:
    hikari:
      connectionTimeout: 30000
      idleTimeout: 600000
      maxLifetime: 1800000
  liquibase:
    change-log: classpath:/liquibase/master.xml
    enabled: true

logging:
  level:
    liquibase: warn

security:
  loginApiBasePath: http://localhost:8080